name: field_ministry
description: "Field Ministry - Gerenciador de territórios e visitas"
publish_to: 'none'

version: 1.2.0+3

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.6
  flutter_slidable: ^3.0.1
  
  # State Management (Riverpod - moderno e type-safe)
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3
  
  # Database (NoSQL)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # Image Picker
  image_picker: ^1.0.7
  
  # Contacts & Url Launcher
  flutter_contacts: ^1.1.9
  url_launcher: ^6.2.5
  
  # Google Sign-In & Drive
  google_sign_in: ^6.2.1
  googleapis: ^12.0.0
  googleapis_auth: ^1.5.1
  http: ^1.2.0
  
  # OAuth2 & Azure AD Authorization
  flutter_appauth: ^11.0.0
  jwt_decoder: ^2.0.1
  
  # Calendar Integration
  device_calendar: ^4.3.1
  
  # Utils
  uuid: ^4.3.3
  path_provider: ^2.1.2
  intl: ^0.20.2
  collection: ^1.18.0
  share_plus: ^10.0.0
  flex_color_picker: ^3.8.0
  universal_html: ^2.3.0
  
  # Charts
  fl_chart: ^0.69.2
  add_2_calendar: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8
  riverpod_generator: ^2.3.9
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/icons/


flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/icons/app_icon.png"
  min_sdk_android: 21


